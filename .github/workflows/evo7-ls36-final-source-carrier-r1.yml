name: ECO EVO7 LS3.6 Final Source Carrier R1

on:
  pull_request:
    branches:
      - feature/eco-evo7-ls36-rule-workbench-r1
    paths:
      - ".github/workflows/evo7-ls36-final-source-carrier-r1.yml"

permissions:
  contents: read

jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact LS3.6 candidate
        uses: actions/checkout@v4
        with:
          ref: 862934ca05aba2b35ba1cab92e11f37f59e271dc
          fetch-depth: 0

      - name: Assert exact target
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "862934ca05aba2b35ba1cab92e11f37f59e271dc"
          test "$(git rev-parse HEAD^)" = "6f8fcd8b959c2832c32747bece454430063ab746"
          test "$(git rev-parse 'HEAD^{tree}')" = "a58649575484a7104ad83647d5510f67c5e12ab6"
          test -z "$(git status --porcelain=v1)"

      - name: Materialize exact source archive
        shell: bash
        run: |
          set -euo pipefail
          tar --exclude=.git -czf "$RUNNER_TEMP/ls36-source-862934c.tar.gz" .
          sha256sum "$RUNNER_TEMP/ls36-source-862934c.tar.gz"

      - name: Upload exact source archive
        uses: actions/upload-artifact@v4
        with:
          name: ls36-source-862934c
          path: ${{ runner.temp }}/ls36-source-862934c.tar.gz
          if-no-files-found: error
          retention-days: 1
