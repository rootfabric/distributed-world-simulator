name: FABRIC-BAKE B0.5-A Exact Source Carrier

on:
  pull_request:
  workflow_dispatch:

jobs:
  bundle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact event subject
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}

      - name: Verify exact source identity
        env:
          EXPECTED_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
        run: |
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          test -z "$(git status --short --untracked-files=no)"
          git diff --check
          echo "HEAD=$(git rev-parse HEAD)"
          echo "TREE=$(git rev-parse 'HEAD^{tree}')"

      - name: Create exact Git bundle
        run: |
          git branch fabric-bake-b0-5-a-exact "$(git rev-parse HEAD)"
          git bundle create fabric-bake-b0-5-a-exact.bundle refs/heads/fabric-bake-b0-5-a-exact
          git bundle verify fabric-bake-b0-5-a-exact.bundle
          sha256sum fabric-bake-b0-5-a-exact.bundle > fabric-bake-b0-5-a-exact.bundle.sha256

      - name: Upload exact source carrier
        uses: actions/upload-artifact@v4
        with:
          name: fabric-bake-b0-5-a-exact-source
          path: |
            fabric-bake-b0-5-a-exact.bundle
            fabric-bake-b0-5-a-exact.bundle.sha256
          if-no-files-found: error
          retention-days: 3
