# Retired under main-owned H0-GIT-TRANSPORT-2026-09-05-R1.
# Historical source recovery remains in Git history; this workflow cannot run transport.
name: FABRIC B0.6 Retired Git Transport
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired. Use normal Git or the authenticated GitHub connector.'
