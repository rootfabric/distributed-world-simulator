name: V0 P5 Exact Source Artifact Probe R5

on:
  pull_request:
    branches:
      - feature/v0-p5-equipment-tools

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Archive exact P5 base source
        shell: bash
        run: |
          set -euo pipefail
          BASE="${{ github.event.pull_request.base.sha }}"
          EXPECTED="ac548f3b3c357fad16f4a8968f943228d705fdce"
          echo "BASE=$BASE"
          test "$BASE" = "$EXPECTED"
          git archive --format=zip --output=p5-source-ac548.zip "$BASE"
      - uses: actions/upload-artifact@v4
        with:
          name: p5-source-ac548
          path: p5-source-ac548.zip
          if-no-files-found: error
          retention-days: 1
