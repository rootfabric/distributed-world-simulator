name: V0 P7.7 Formal Source Snapshot R1

on:
  pull_request:
    branches:
      - feature/v0-p7-bounded-terrain-mutation
    paths:
      - ".github/workflows/v0-p7-7-formal-source-snapshot-r1.yml"
  workflow_dispatch:

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: acbf4fd3c5b6205584d8c2d139011894034408ed
          fetch-depth: 0
      - name: Verify exact identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "acbf4fd3c5b6205584d8c2d139011894034408ed"
          test "$(git rev-parse 'HEAD^{tree}')" = "311b7779f1a9eaa67c77406fe1604fdb0f9d5b00"
          test -z "$(git status --porcelain --untracked-files=no)"
      - name: Archive source
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .git
          tar -czf /tmp/dws-p7-7-formal-acbf4fd3.tar.gz .
          sha256sum /tmp/dws-p7-7-formal-acbf4fd3.tar.gz
      - uses: actions/upload-artifact@v4
        with:
          name: dws-p7-7-formal-acbf4fd3-source
          path: /tmp/dws-p7-7-formal-acbf4fd3.tar.gz
          if-no-files-found: error
          retention-days: 1
