# NX2 Realtime Traffic Separation — fix1

```text
checkpoint: v16.12.0-network-nx2-realtime-traffic-separation
delivery: fix1
branch: feature/nx2-realtime-traffic-separation
apply over: NX2 candidate
status: CANDIDATE_FIX1
```

Закрыты review-дефекты:

- потеря authoritative movement acknowledgement после потери unreliable snapshot;
- отсутствие проверки физического ENet channel/transfer mode;
- trailing whitespace в общей дорожной карте.

Ключевая политика подтверждения: клиент повторяет неподтверждённый input batch, сервер при redundant batch ограниченно перепубликует текущий authoritative snapshot. Успешные movement `COMMAND_RESULT` не возвращаются.
