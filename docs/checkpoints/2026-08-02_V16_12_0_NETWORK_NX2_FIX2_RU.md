# NX2 Realtime Traffic Separation — fix2

```text
checkpoint: v16.12.0-network-nx2-realtime-traffic-separation
delivery: fix2
branch: feature/nx2-realtime-traffic-separation
apply over: NX2 candidate + fix1
status: CANDIDATE_FIX2
```

Закрыты review-дефекты fix1:

- физическое channel/mode нарушение больше не переводит весь server boundary в `FAILED`;
- offending peer получает peer-local quarantine и отключается;
- его session фиксирует точный failure code, healthy peers и listener продолжают работу;
- PowerShell focused runner действительно выполняет physical-channel process regression и соответствует заявленным `9/9` шагам.

Real-ENet regression использует два предварительно зарегистрированных клиента. После нарушения второй клиент продолжает отправлять валидные frames с прежним logical peer ID.

Прямые controlled Godot-прогоны являются вспомогательными. Финальное решение `ACCEPTED` требует независимого managed-MCP прогона после editor import.
