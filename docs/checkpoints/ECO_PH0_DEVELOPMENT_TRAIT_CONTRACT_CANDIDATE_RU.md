# ECO.PH0 — Development Trait Contract — CANDIDATE

Статус: `LOCAL_FOCUSED_PASS / EXACT_WINDOWS_PENDING`.

PH0 фиксирует границы до появления morphology generator: наследуемые developmental traits, deterministic `IndividualSeed`, `SeedGenomeEnvelope`, отдельный `DevelopmentState` и derived `GrowthGraph`.

- 8 initial developmental traits;
- `TREE/BUSH/GRASS` не являются canonical type;
- renderer/mesh не входит в genome contract;
- DevelopmentState не меняет genome;
- GrowthGraph явно `derived_representation`;
- population truth и production persistence/authority/network остаются вне PH0.

Local evidence: `63 assertions`, `0 failures`.

- development traits hash: `9d812950f421c2618ce0c62aa30e417e953dd9a61abdc14a03f9d129df876dea`;
- deterministic individual seed: `959597643576420676`.

После exact-Windows PASS PH0 принимается и формально разблокирует PH1 acceptance.
