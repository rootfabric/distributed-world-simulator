# ECO EVO3 E3.7 — REPAIR R1 POST-BUILD CRITIQUE

Статус: `IMPLEMENTER REPAIR EVIDENCE COMPLETE / AWAITING FRESH INDEPENDENT REVIEW`.

Fresh independent Reviewer на exact `97d01ec3cca450709af53a80c233970ee1187d62` вернул `FIX_REQUIRED` по finding `E3.7-R1-001` (durable PR comment `#5369696271`).

## Корневая причина

В production module существовал generic helper `serialized_bytes(v: Any)`, который мог получить plain reconstructed `dict` и сформировать ровно тот же финальный `SORTED_KEYS_COMPACT_UTF8_NEWLINE_V1` byte format, что и защищённый serializer. Поэтому `_VerifiedPlanetEcologyProgram`, `validate_output_integrity()`, retained raw inputs и independent rebuild можно было обойти.

Первоначальный test проверял только rejection через `serialize_planet_ecology_program(plain)` и не перечислял sibling/module finalization surfaces. По этой же причине первоначальный critique `E3-7-POST-BUILD-CRITIQUE-001` ошибочно заключил `NO_MATERIAL_REFACTOR_REQUIRED`; он сохраняется как историческое evidence и не является authority для Repair R1.

## Repair R1

Exact repair executable freeze:

`b30f7a9cfb54a673c6249be1e5f9a3c7f60042c9`

Repair ограничен authority surface:

- `serialized_bytes` удалён из production module;
- generic `canonical_bytes` остаётся newline-free primitive для hashing/canonical comparison;
- финальный newline-format формируется только внутри `serialize_planet_ecology_program()` после `validate_output_integrity()`;
- `write_planet_ecology_program()` использует только защищённый serializer;
- authority tests теперь перечисляют все public module functions, содержащие `serial/final/write`, и требуют ровно два допустимых entry point: `serialize_planet_ecology_program`, `write_planet_ecology_program`;
- plain reconstructed, forged production-authority и mutated dict должны быть отклонены каждым finalization surface;
- rejected writer не должен создавать output file.

Accepted E3.1–E3.6 inputs, persisted EVO2 SpeciesCatalog и generated scientific `PlanetEcologyProgram` не изменялись.

## Machine validation

Repair Closure `32481325987 / #9`, job `96768126271` — SUCCESS на exact `b30f7a9c...`:

- tests `30/30 PASS`;
- predicates `16/16 PASS`;
- `NO_ALTERNATE_PROGRAM_SERIALIZER_HELPER = PASS`;
- public finalization surfaces = `serialize_planet_ecology_program,write_planet_ecology_program`;
- schema PASS;
- fresh-process builds `2/2` byte/hash-identical;
- committed generated bytes identical.

Generated artifact остался тем же:

- bytes `104186`;
- SHA-256 `52ff70fddc7f05fde00e5159f38dd8e67def3e732b03c93a15bedce540dae303`;
- Git blob `01207a7025710db34fafc959fcc26dade2606d89`;
- provenance `832a2d1c5b78ceda2f674843b4e6ee7052f5bd8a8400181aec7aeb029b472eff`;
- program hash `b405d35ebd8bebcc3218249fe78e495b94f4098eb06673eebc3e6475ea7a4956`.

Project Control `32481326131 / #1121`, job `96768126542` — SUCCESS. Overall/ECO/directional остаются RED; `NX -> ECO` YELLOW WATCH_HIT; critical ECO intersection отсутствует; critical RED остаются `NX -> V0` и `V0 -> NX`.

## Gate

Это implementer-side repair evidence, а не Reviewer PASS. После evidence-only commit требуется fresh exact-head Closure + Project Control и новый fresh independent Repair R1 Reviewer. Verifier до Reviewer PASS заблокирован. E3.8, XFER1 и production ECO остаются blocked/inactive.
