# FABRIC1 — GENERALIZED WORLD FABRIC

## Status

`RESEARCH EXACT CLOSED`

FABRIC1 is the integration milestone after B0.7. It does not introduce another physical law. It proves that the generic FABRIC/BAKE mechanisms can be used together as one bounded world-physics substrate.

## Definition

```text
FABRIC1
= Constructible
+ Composable
+ Hybrid
+ Persistent
+ Reducible
+ Refinable
+ Deterministic
+ Causally scalable
```

## New integration surface

- `fabric1_component_contract_v1.gd` — generic component-as-data contract;
- `fabric1_generalized_composer_v1.gd` — deterministic generic port composition;
- `fabric1_generalized_runtime_v1.gd` — BAKED/FULL/refine/failure/rebake/restart lifecycle;
- sharded exact core and computational-scale acceptance.

The new layer reuses the existing B0.7 exact generic compiler and B0.1 reduction/runtime contracts. Canonical mutations stay external. FABRIC1 artifacts and capsules never become canonical world truth.

## Executable integration path

```text
construct components
      ↓
compose through physical ports
      ↓
compile FULL graph
      ↓
automatic exact BAKE
      ↓
execute cheaply
      ↓
refinement guard requests FULL
      ↓
external canonical topology change
      ↓
stale representation fenced
      ↓
FULL successor
      ↓
automatic REBAKE
      ↓
derived restart/replay
```

## Scale interpretation

FABRIC1 combines two kinds of causal scaling:

- computational reduction: internal equation count grows while boundary BAKE dimension remains bounded;
- spatial/local refinement: COMPLEX3 already proves 100k canonical parts with only 20 active FULL parts after a local event.

The second proof is inherited byte-identically rather than wastefully rerun inside every FABRIC1 validation.

## Important boundary

`RESEARCH EXACT CLOSED` is not `PRODUCTION ACCEPTED`. Global Project Control remains RED because of unrelated active product lines. Broader arbitrary nonlinear, thermal, hydraulic or fully multi-domain automatic synthesis remains future FABRIC work.

Exact evidence: `validation/FABRIC1_GENERALIZATION_CLOSURE.md` and `validation/fabric1/exact-closure.v1.json`.
