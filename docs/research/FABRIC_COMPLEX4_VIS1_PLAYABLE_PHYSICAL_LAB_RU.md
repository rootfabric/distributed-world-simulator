# COMPLEX4-VIS1 — Playable Physical Lab

Status: **RESEARCH EXACT CLOSED**.

COMPLEX4-VIS1 makes the closed COMPLEX4 causal chain directly observable in Godot without introducing new canonical physics.

```text
canonical Construction/Matter
        ↓
COMPLEX4 / BRIDGE-4 / FABRIC1
        ↓
playable 3D observatory
```

The scene intentionally exposes the truth boundary: an overload first creates a noncanonical physical proposal and refines to FULL while the canonical revision stays unchanged; only the next external Construction mutation breaks the support and advances the revision.

Scene:
`res://scenes/labs/fabric/complex4_playable_physical_lab.tscn`

Launcher:
`RUN_FABRIC_COMPLEX4_VIS1_LAB.sh`

Controls: `SPACE` proof sequence, `1` primary, `2` backup, `N` restart, `R` reset.

Expected visible sequence:

```text
ON / 2 paths
→ proposal FULL / revision unchanged
→ ON / 1 path / revision 1
→ proposal FULL / revision unchanged
→ OFF / 0 paths / revision 2
→ restart stays OFF
```

Closure hash: `ba403c46263740af161bc69778b4abe58fa19fe6bb2983884e9f8dd19c325f16`.
