# FABRIC.SYNC4 — POST-B0.5-A + COMPLEX0/1A DEVELOPMENT REVIEW

## Status

```text
FABRIC.SYNC4
POST-B0.5-A FABRIC SYNC

B0.5-A EXECUTABLE HYBRID BAKE:
CLOSED

COMPLEX0 BREAKABLE STRUCTURE:
CLOSED

COMPLEX0-PERF1 — B0.2-E TOPOLOGY RE-BAKE SCALING:
CLOSED

COMPLEX0 @ 2000:
EXACT DOUBLE VERIFIED / CLOSED

COMPLEX1A POWERED FENCE FULL CAUSAL BASELINE:
CLOSED

FABRIC0.19:
NOT AUTHORIZED

BRIDGE-2 EXECUTABLE RESEARCH:
AUTHORIZED

PRODUCTION:
NOT ACCEPTED
```

This review is the synchronization boundary requested after B0.5-A. It does not
change Physical Core, B0.2-E, B0.4 or B0.5 runtime semantics. It records the
executable evidence accumulated after SYNC-3 and authorizes the next research
bridge only where the previous blockers are now demonstrated.

## Exact implementation subject

```text
branch:
research/fabric-bake0-5-a-executable-hybrid-r2

HEAD:
5f1d2dc997ecce5cf1f188a6f65d7b1c2ba0ecd9

TREE:
d094c969a4252eb77f697d142271b9f74c3f1589

tracked worktree:
CLEAN
```

The exact source came from the successful GitHub Exact Source Carrier for this
same HEAD and was checked out detached from the produced bundle before local
runtime verification.

## Canonical double Godot

```text
version:
4.7.1.stable.double.custom_build.a13da4feb

binary SHA-256:
bfa7ce632d8d4b1dcc96f64f5405ee52b57c4e25d15c3e0478acc26e08d517d7

archive SHA-256:
d7a184b893d4e3ad4d4b6cb2e3a4fbb52997dfc87e4f00d2a7f24ac075903b92
```

Exact verification host:

```text
OS:
Debian GNU/Linux 13 (trixie)

kernel:
Linux 6.18.35 x86_64

CPU:
AMD EPYC 9V74 80-Core Processor
```

This host is not promoted as the project's independent self-hosted Ubuntu runner.
The exactness claim here is about subject identity + canonical double Godot +
clean tracked tree. The queued self-hosted Linux workflow remains supplementary
independent evidence and is not substituted with a fake SUCCESS.

## COMPLEX0 closure

The permanent ladder now proves:

```text
50 parts
→ FULL floor
→ safe bake refusal

100 parts
→ BAKE_READY
→ split would leave a component below 100
→ safe FULL fallback

500 parts
→ BAKE
→ guard
→ canonical break
→ invalidation
→ stale reject
→ split
→ two rebaked fragments
→ reconstruction
→ PASS

2000 parts
→ BAKE
→ guard
→ canonical break
→ RepresentationInvalidation
→ BakeInvalidation
→ old artifact STALE
→ B0.2-E topology transaction
→ split 993 + 1007
→ two fresh PhysicalBakeArtifact values
→ TopologyRuntime APPLIED
→ conservation
→ reconstruction 2000/2000
→ PASS
```

Exact results:

```text
50:
5 assertions PASS

100:
11 assertions PASS

500:
54 assertions PASS
fragments = 243 + 257
post-split reduction ratio = 250x

2000:
25 assertions PASS
fragments = 993 + 1007
post-split reduction ratio = 1000x
transaction checksum =
b421928b2a5db9e700476d9d70e2be6b6ecde8bfe778b2511a1efaf85d082ebd
max reconstructed state error =
~2e-14
```

Therefore the COMPLEX0 lifecycle is closed through the planned 2000-part rung.

## COMPLEX0-PERF1 decision

The original long-wall-clock observation did not reproduce as a B0.2-E compiler
scaling defect when measured on a clean exact subject.

Permanent PERF1 contract:

```text
500 topology transaction compile:
< 10 s

2000 topology transaction compile:
< 60 s

4x canonical part growth:
transaction compile growth <= 8x

500 and 2000 transaction checksums:
exact deterministic
```

Measured exact result on this subject:

```text
500:
build_ms        = 79
structural_ms   = 1809
break_ms        = 84
transaction_ms  = 1967
checksum        =
a606954658e0ab87eca2cb1b6ff2280b4ad7ebd56f125e3fbad3767e103ed828

2000:
build_ms        = 306
structural_ms   = 7699
break_ms        = 336
transaction_ms  = 8647
checksum        =
b421928b2a5db9e700476d9d70e2be6b6ecde8bfe778b2511a1efaf85d082ebd

part growth:
4.0x

transaction compile growth:
4.39603457041179x

PERF1:
19 assertions PASS
```

The pathological old wall-clock came from the acceptance harness retaining a
large transaction graph and compiling a second full 2000-part transaction while
the first remained live, plus process teardown/allocator pressure. The permanent
harness now uses fresh processes and frozen deterministic checksums. No B0.2-E
physical/compiler semantic change was required.

## COMPLEX1A causal baseline

```text
FABRIC-BAKE COMPLEX1A Acceptance:
93 assertions PASS
```

The tested causal chain is:

```text
mechanical support topology
        ↓
functional connectivity
        ↓
generic electrical-like effort/flow solve
        ↓
load absorbed power
        ↓
load ON/OFF
```

The fixture also rejects the forbidden hardcode pattern:

```text
fence_broken → lamp_off()
```

because it proves unrelated-break, redundant-path and two-load locality cases.

## Control evidence on implementation subject

```text
Project Control:
33727368612
SUCCESS

B0.5-A Exact Source Carrier:
33727368693
SUCCESS

Exact Source Carrier artifact:
9882476413

Complex Labs Portable Smoke:
33727368928
SUCCESS

Complex Labs Linux Double Verification:
33727368671
QUEUED on project self-hosted runner at synchronization time
```

Portable Godot is parser coverage for COMPLEX0 and executable causal smoke for
COMPLEX1A. It is not used to claim authoritative COMPLEX0 runtime correctness.

## FABRIC0.19 necessity review

Verdict:

```text
FABRIC0.19
NOT AUTHORIZED
```

No executable falsifier in B0.5-A, COMPLEX0, COMPLEX0-PERF1 or COMPLEX1A proves a
missing generic Physical Core primitive.

The only newly discovered failure mode was acceptance-harness lifetime / memory /
teardown behavior. It was repaired in the test/control layer without adding a
new physical primitive and without weakening a physical tolerance.

Therefore an FABRIC0.19 proposal still requires a future concrete executable
failure that cannot be represented by the current generic FLOW/JUMP/topology,
conservation, invalidation and bake lifecycle semantics.

## BRIDGE-2 executable authorization review

SYNC-3 required the following before executable BRIDGE-2:

1. B0.5-A CLOSED;
2. an actual B0.4-backed lazy-compiled mode artifact;
3. exact mode transition/state handoff evidence;
4. explicit ownership for FULL / STRUCTURAL_BAKE / CONTACT_BAKE / DYNAMIC_ROM /
   HYBRID_BAKE;
5. common invalidation/refinement ordering;
6. deterministic mixed replay;
7. no hidden authority crossing.

The predecessor implementation now supplies the missing executable B0.5-A
transition/cache/handoff evidence, while COMPLEX0 supplies a real topology
mutation/invalidation/split/rebake lifecycle and COMPLEX1A supplies the causal
FULL oracle for the first mixed-system falsifier.

Decision:

```text
BRIDGE-2 EXECUTABLE RESEARCH:
AUTHORIZED
```

This is not BRIDGE-2 CLOSED and is not production authorization.

BRIDGE-2 must now prove one canonical object/system can safely compose multiple
derived physical representations while preserving one owner for each event and
one canonical source of truth.

## First authorized BRIDGE-2 falsifier

The first executable target is COMPLEX1B:

```text
battery
   │
wire / functional path
   │
structural fence
   │
lamp/load

stable fence region      → STRUCTURAL_BAKE
local impact region      → FULL
eligible stable dynamics → DYNAMIC_ROM / HYBRID_BAKE
functional path          → generic FABRIC execution
```

Required event chain:

```text
impact
→ local guard
→ local refinement / FULL
→ canonical structural break
→ dependent wire/path topology mutation
→ exact invalidation
→ electrical re-solve
→ lamp state changes only if causal connectivity requires it
```

Primary oracle:

```text
COMPLEX1A FULL causal outcome
==
COMPLEX1B mixed FULL/BAKED/ROM/HYBRID outcome
```

Secondary anti-hardcode target is CX2 Redundant Power Fence.

## Final decision

```text
════════════════════════════════════════
FABRIC.SYNC4 — POST-B0.5-A FABRIC SYNC
════════════════════════════════════════

B0.5-A                    ✅ CLOSED
COMPLEX0                  ✅ CLOSED
COMPLEX0-PERF1            ✅ CLOSED
COMPLEX0 @ 2000           ✅ EXACT CLOSED
COMPLEX1A                 ✅ CLOSED

FABRIC0.19                ⛔ NOT AUTHORIZED
BRIDGE-2 EXECUTABLE       ✅ RESEARCH AUTHORIZED

PRODUCTION                ⛔ NOT ACCEPTED
════════════════════════════════════════

NEXT:
BRIDGE-2 executable
→ COMPLEX1B Powered Fence Mixed
→ CX2 Redundant Power Fence
```
