# WF0.10 Observe / Showcase Toolkit — Validation — 2026-09-03

Godot:
`4.7.1.stable.double.custom_build.a13da4feb`

Worktree:
`C:\distributed-world-simulator\world-fill-r1` (branch
`feature/world-fill0-noncanonical-world-enrichment-r1`, base `e8df791e` WF0.9)

Deliverables:
- `scripts/world_fill/showcase/world_fill_showcase_kit.gd` — repeatable
  demo/visual-regression observations: `capture_observation()` bundles the
  caller-supplied read-only context (screenshot reference, simulation tick,
  region, authority, checksum, world-fill preset, scene id) into a fixed
  schema without wall-clock time; missing optional fields become explicit
  `MISSING_*` reasons; six canonical spectator bookmarks
  (spawn/outpost/dig_site/seam/handoff/horizon) with position+target;
  records persist locally under `user://world_memory/showcase/<id>.json`;
- `tests/world_fill/test_wf0_10_showcase.gd` — 6 scenario groups;
- `RUN_WORLD_FILL_WF0_10_TESTS.ps1` — full chained runner (WF0.1 → WF0.10
  tests + both lab scenes headless);
- demo `_build_showcase_record()` captures and saves `demo_last_run`.

Validation (runner `RUN_WORLD_FILL_WF0_10_TESTS.ps1`, all steps PASS):
1. bookmarks: exactly the six roadmap viewpoints, sorted, deterministic;
2. full observation capture keeps tick/preset/checksum and is `complete`;
3. fail-soft: empty input yields incomplete record with
   MISSING_SIMULATION_TICK + MISSING_WORLD_FILL_PRESET, empty screenshot;
4. deterministic capture: identical inputs → identical records;
5. save/load roundtrip through user:// preserves the record (JSON int→float
   normalization handled numerically; one test-side fix applied);
6. cleanup + save guards: empty id and foreign schema refuse to save.
Regression guards WF0.1-WF0.9 PASS. Demo: all counters +
`WORLD_FILL_SHOWCASE_COMPLETE=true`, `WORLD_FILL_SHOWCASE_SAVED=true`,
sentinel `WORLD_FILL_DEMO_READY`.

## Constitutional gates

- CANON-INDEPENDENT: observations are local records; nothing canonical reads
  them.
- NO-NEW-WRITER: user:// showcase storage only; caller supplies all values.
- FAIL-SOFT: missing fields become reasons; invalid saves are refused.
- DETERMINISTIC-WHEN-CLAIMED: no wall-clock in records; identity from
  caller-supplied tick/ids.
- BUDGETED: one JSON file per observation id.
- DEMO-VISIBLE: the demo captures and saves its own observation each run.
- LICENSE-CLEAN: no third-party content.

## Scope note

Staged paths: `scripts/world_fill/`, `tests/world_fill/`,
`RUN_WORLD_FILL_WF0_10_TESTS.ps1`, `docs/world_fill/`.
