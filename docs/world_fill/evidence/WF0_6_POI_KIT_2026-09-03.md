# WF0.6 Landmarks / POI Kit — Validation — 2026-09-03

Godot:
`4.7.1.stable.double.custom_build.a13da4feb`

Worktree:
`C:\distributed-world-simulator\world-fill-r1` (branch
`feature/world-fill0-noncanonical-world-enrichment-r1`, base `a0371e10` WF0.5)

Deliverables:
- `scripts/world_fill/landmarks/world_fill_poi_kit.gd` — hand-authored,
  asset-free procedural landmark fixtures for all nine roadmap POIs
  (outpost, antenna, wreck, mining_camp, research_station,
  cave_entrance_marker, landing_site, radio_beacon, broken_pipeline), each
  built from primitive meshes plus an optional billboard Label3D nameplate;
- budget: `MAX_POIS = 32`, oldest evicted first;
- eligibility gate: `require_eligible` consumes the WF0.1 `poi_eligibility`
  hints through an explicit kind→hint vocabulary map
  (`cave_entrance_marker`→`cave_entrance`, `radio_beacon`→`beacon`; kinds
  without a contract hint default to eligible);
- `tests/world_fill/test_wf0_6_poi_kit.gd` — 6 scenario groups;
- `RUN_WORLD_FILL_WF0_6_TESTS.ps1` — chained runner (WF0.1 → WF0.6 → demo);
- demo `_build_landmarks()` spawns landing_site + radio_beacon.

Validation (runner `RUN_WORLD_FILL_WF0_6_TESTS.ps1`, all steps PASS):
1. all nine kinds spawn, each carrying exactly one label;
2. budget eviction at 32 keeps the newest, frees the oldest node;
3. eligibility gate: eligible outpost spawns, ineligible cave marker is
   skipped with INELIGIBLE and creates no node (uses a real WF0.1 decision);
4. unknown kind fails soft with UNKNOWN_POI_KIND and no node;
5. report exposes only presentation keys;
6. fixtures carry recognizable multi-mesh content.
Regression guards WF0.1-WF0.5 PASS. Demo: `WORLD_FILL_AMBIENCE=clear`,
`WORLD_FILL_SCATTER_INSTANCES=384`, `WORLD_FILL_SCARS_ACTIVE=3`,
`WORLD_FILL_FEEDBACK_UI=1`, `WORLD_FILL_POIS=2`,
sentinel `WORLD_FILL_DEMO_READY`.

One integration defect was found and fixed during validation: the POI kind
vocabulary and the WF0.1 eligibility-hint vocabulary diverged for two kinds
(cave_entrance_marker/radio_beacon vs cave_entrance/beacon); the kit now
maps kinds to contract hint keys explicitly, keeping the contract unchanged.

## Constitutional gates

- CANON-INDEPENDENT: fixtures are lab/demo children only.
- NO-NEW-WRITER: placement is explicit caller input; no authority owner.
- FAIL-SOFT: unknown kinds and ineligible placements skip with reasons.
- DETERMINISTIC-WHEN-CLAIMED: spawn sequence fully determines the result.
- BUDGETED: 32 fixtures cap, asserted.
- DEMO-VISIBLE: two POIs observable in the demo scene with nameplates.
- LICENSE-CLEAN: primitives + Label3D only, no third-party content.

## Scope note

Staged paths: `scripts/world_fill/`, `tests/world_fill/`,
`RUN_WORLD_FILL_WF0_6_TESTS.ps1`, `docs/world_fill/`.
