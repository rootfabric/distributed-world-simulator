# WF0.9 Labels / Signs / Identity — Validation — 2026-09-03

Godot:
`4.7.1.stable.double.custom_build.a13da4feb`

Worktree:
`C:\distributed-world-simulator\world-fill-r1` (branch
`feature/world-fill0-noncanonical-world-enrichment-r1`, base `cea01e5e` WF0.8+scout)

Deliverables:
- `scripts/world_fill/labels/world_fill_sign_kit.gd` — local, fixture-backed
  text labels/signposts: five styles (location_name, outpost_label,
  container_label, poi_label, warning_sign), single-line sanitized text
  (64-char cap, newlines flattened), optional signpost pole, billboard
  Label3D; budget `MAX_SIGNS = 64`, oldest evicted first;
- `tests/world_fill/test_wf0_9_sign_kit.gd` — 6 scenario groups;
- `RUN_WORLD_FILL_WF0_9_TESTS.ps1` — chained runner (WF0.1 → WF0.9 → demo);
- demo `_build_signs()` adds a location name and a container label.

Validation (runner `RUN_WORLD_FILL_WF0_9_TESTS.ps1`, all steps PASS):
1. all five styles spawn with exactly one Label3D each;
2. sanitization: 64-char cap enforced, newlines flattened;
3. budget: 74 created → exactly 64 kept, oldest evicted first, nodes freed;
4. fail-soft: whitespace-only text skipped with EMPTY_TEXT; unknown style
   falls back to location_name and still spawns;
5. report exposes only presentation keys;
6. clear_signs resets records and nodes.
Regression guards WF0.1-WF0.8 PASS. Demo: counters +
`WORLD_FILL_SIGNS=2`, sentinel `WORLD_FILL_DEMO_READY`.

R1 is local/fixture-backed by design; networked-editable signs remain a
STOP CONDITION and require a canonical proposal.

## Constitutional gates

- CANON-INDEPENDENT: labels are visual only.
- NO-NEW-WRITER: no authoritative annotation store; local callers only.
- FAIL-soft: invalid text/style degrade with explicit reasons.
- DETERMINISTIC-WHEN-CLAIMED: spawn sequence determines the result.
- BUDGETED: 64 signs, asserted.
- DEMO-VISIBLE: two signs observable in the demo with a counter.
- LICENSE-CLEAN: Label3D/primitives only.

## Scope note

Staged paths: `scripts/world_fill/`, `tests/world_fill/`,
`RUN_WORLD_FILL_WF0_9_TESTS.ps1`, `docs/world_fill/`.
