# V0-P5 R2 V002 runtime evidence R1

Evidence-only raw bundle for PR #173 exact evidence head `5434558856c00b588eed5369d2c613cd4b9858bb`.
Runtime/test bytes are the immutable freeze `5c3d936b35ecaa6b6023aac0e251f56858852186`; the only later candidate change before `543455...` is append-only control event metadata, and the two changed runtime/test Git blobs were independently re-read from `543455...` before publication.

The bundle contains the ordered canonical 239-test manifest, full orchestration console, hashes for all 239 per-test raw logs, critical raw logs, `main_scene_cli_all`, clean import, seed regression, direct/repeated/stressed M7 recovery evidence, and ordered prefix/neighbor evidence.

Historical R2 PRODUCT_RUNTIME_RED on `601ca87...` is not overwritten by this bundle.
